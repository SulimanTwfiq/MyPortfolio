import React from "react";
import styled from "styled-components";

const StyledSlider = styled.section`
  grid-area: slider;
  background-color: lightgray;
`;
const Slider = () => <StyledSlider>Slider</StyledSlider>;

export default Slider;
